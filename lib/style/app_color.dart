import 'package:flutter/material.dart';

class AppColor {
  // Core brand / actions (coral similar to reference screens)
  static const primary = Color(0xffF58B7C);

  // Secondary accents
  static const primaryBlue = Color(0xff2F3136); // deep neutral for icons/text
  static const background = Color(0xffF9FAFB); // soft off-white
  static const cardBackground = Color(0xffFFFFFF);

  // Text hierarchy
  static const fontTitle = Color(0xff1F1F1F); // near-black for titles
  static const fontBody = Color(0xff5F5F6B); // muted gray for body
  static const fontLight = Color(0xffB8BCC4); // subtle tertiary text

  // Accent colors for states
  static const accentGreen = Color(0xff4CAF50);
  static const accentOrange = Color(0xffFFB88C);

  // UI states
  static const disableFont = Color(0xffA7A7A7);
  static const disableButton = Color(0xffE5E7EB);

  // Special backgrounds
  static const footerBackground = Color(0xff2F3136);
  static const accentBackground = Color(0xffEDEEEF);

  // Essentials
  static const black = Color(0xff000000);
  static const white = Color(0xffffffff);

  // Utility
  static const fieldColor = Color(0xffF3F4F6);
  static const borderColor = Color(0xffE6E8EC);
}
